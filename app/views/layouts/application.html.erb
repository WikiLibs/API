<!DOCTYPE html>
<html>
  <head>
    <title>WikiLibs</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>

  <% if user_signed_in? %>

  <p> Hello, <%= current_user.email %> - <%= link_to("Sign_out", destroy_user_session_path, method: :delete) %> </p>

  <br />
  <% end %>

    <%= yield %>
  </body>
</html>
